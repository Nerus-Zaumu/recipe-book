import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scrapRecipeBook',
  appName: 'recipe-book',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
